apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: render-api
  namespace: rendering
spec:
  secretName: render-api-tls
  issuerRef:
    name: letsencrypt-http
    kind: ClusterIssuer
  dnsNames:
    - render.local
