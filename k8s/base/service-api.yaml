apiVersion: v1
kind: Service
metadata:
  name: render-api
  namespace: rendering
  labels:
    app.kubernetes.io/name: render-api
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: render-api
  ports:
    - name: http
      port: 8080
      targetPort: http
