apiVersion: v1
kind: Service
metadata:
  name: blender-worker-metrics
  namespace: rendering
  labels:
    app.kubernetes.io/name: blender-worker
    app.kubernetes.io/component: worker
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: blender-worker
  ports:
    - name: metrics
      port: 9100
      targetPort: metrics
