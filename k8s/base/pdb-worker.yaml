apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: blender-worker
  namespace: rendering
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: blender-worker
